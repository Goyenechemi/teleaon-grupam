/**
 * Full-screen fixed overlay that creates a radial-gradient glow
 * that follows the mouse cursor. CSS custom properties --mouse-x
 * and --mouse-y are updated by the useMouseGlow hook.
 */
export function MouseGlow() {
  return <div id="mouse-glow" />;
}
